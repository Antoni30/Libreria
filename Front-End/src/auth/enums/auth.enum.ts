export enum LoginActionType {
  SET_EMAIL,
  SET_PASSWORD,
  SUBMIT,
  RESET,
}

export enum RegisterActionType {
  SET_FULLNAME,
  SET_EMAIL,
  SET_PHONE_NUMBER,
  SET_PASSWORD,
  SUBMIT,
  RESET,
}
